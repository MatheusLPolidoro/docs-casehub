# Project structure

Tree of the repositories this site documents, generated at build time.

<h2>fast-casehub (API)</h2>
<p><em>Repository not found at <code>../fast-casehub</code> at build time — the tree is generated from the sibling repositories, when they are present.</em></p>

<h2>casehub-connect (SDK)</h2>
<p><em>Repository not found at <code>../casehub-connect</code> at build time — the tree is generated from the sibling repositories, when they are present.</em></p>
