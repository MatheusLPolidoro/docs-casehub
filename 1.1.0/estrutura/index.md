# Estrutura dos projetos

Árvore dos repositórios que este site documenta, gerada no build.

<h2>fast-casehub (API)</h2>
<p><em>Repositório não encontrado em <code>../fast-casehub</code> no momento do build — a árvore é gerada a partir dos repositórios irmãos, quando presentes.</em></p>

<h2>casehub-connect (SDK)</h2>
<p><em>Repositório não encontrado em <code>../casehub-connect</code> no momento do build — a árvore é gerada a partir dos repositórios irmãos, quando presentes.</em></p>
